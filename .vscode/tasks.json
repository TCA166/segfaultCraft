{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Make build",
            "command": "make",
            "args": ["CFLAGS='-g'"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build",
            "detail": "Task generated by me :)"
        }
    ],
    "version": "2.0.0"
}